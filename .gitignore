__pycache__/
*.py[cod]
node_modules/
# Run outputs: regenerated by the two phases, never reviewed as source.
results.json
eval-report.json
strands_eval_report.json
artifacts/
.venv/
.eval/
# gh-pages worktree used by evalkit.pages when publishing the scoreboard locally.
/site/
*.egg-info/
/dist/

# Suffixed run outputs (results.<suite>.json / eval-report.<suite>.json), same as above.
results.*.json
eval-report.*.json

# Diagrams rendered at the repo root while using a skill: one-off deliverables for
# whoever asked, not skill source. The eval's own fixtures live under
# skills/*/eval/reference/ and are tracked there.
/*.webp
/*.gif
/*.png
/*.spec.json

# Playwright MCP scratch output (page snapshots, console logs)
.playwright-mcp/

# AWS-internal skill: kept locally, never published.
skills/essr-html-report/

# macOS metadata and per-machine Claude Code permissions.
.DS_Store
.claude/settings.local.json
